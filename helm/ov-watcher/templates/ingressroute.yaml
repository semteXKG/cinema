apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ov-watcher
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{ .Values.ingress.host }}`)
      kind: Rule
      services:
        - name: ov-watcher
          port: 8080
  tls:
    secretName: ov-watcher-tls
