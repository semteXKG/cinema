apiVersion: v1
kind: Service
metadata:
  name: ov-watcher
  labels:
    app: ov-watcher
spec:
  type: ClusterIP
  selector:
    app: ov-watcher
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
